{
    "name": "setupmate",
    "version": "1.0.0",
    "description": "A tool to track your system setup progress across different platforms.",
    "scripts": {
      "build": "npm run minify",
      "minify": "terser src/app.js -o dist/app.min.js && cleancss -o dist/styles.min.css src/styles.css"
    },
    "dependencies": {
      "terser": "^5.43.1",
      "clean-css-cli": "^5.6.3"
    },
    "author": "Fynks"
  }